# ML model weights

Runtime downloads:

- torchvision EfficientNet-B0 / ViT-B/16 ImageNet weights (first use)
- open_clip ViT-B-32 openai weights (first use)

Binaries under this tree are gitignored. Feature extractors live in `app/ml/`.

`DOCSHIELD_ML_MODE=mock` skips downloads for tests.
